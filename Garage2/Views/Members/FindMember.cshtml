

@model IEnumerable<Garage2.Models.Member>

@{
    ViewBag.Title = "FindMember";
}

<h2>Search Result</h2>

<p>

</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            
            
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Check out", "Checkout", new { id = item.Id })
            </td>
            <td>
                @Html.ActionLink("Park your Car", "CreateWithMember", "Vehicles", new { id = item.Id }, null) 
                
            </td>
        </tr>
    }

</table>

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
<br>
<div>
    @Html.ActionLink("Search again", "Search")
</div>
